# Changelog

## [Unreleased]

## [0.1.1] — 2026-08-15

Initial public release:

- `mathshow` CLI — KaTeX browser preview from args, files, or stdin
- `show-math` skill — agents run `mathshow` when the user should see formulas

Tag `v0.1.1` from `main` after publish when you want a pin-able release.
